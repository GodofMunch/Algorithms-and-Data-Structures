/*//Eratosthenes.java
public class Erathosthenes {
    public static int[] primeList(int n) {

       /* double squareRootOfN = Math.sqrt(n);
        int[] numberList = new int[n];
        int a;
        int multiplier;

        for(int k = 0; k < numberList.length; k++) {
            numberList[k] = k;
        }

        for(int i = 2;i < numberList.length; i++){
            a = i;

            if(MyMath.isPrimeNumber(a)){
                multiplier = a;
            }
                for(int j = a; j < n; j++) {
                    multiplier += multiplier + a;

                    if(n<numberList.length)
                        numberList[multiplier]=0;
                }
        }
        int counter;

        for(counter = 0;counter < numberList.length; counter ++) {
            if()
        }

        return answer;
    }
}*/
